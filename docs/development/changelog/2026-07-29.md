# 2026-07-29

## Tag-dossié kézi QA — LEZÁRVA (+ streamelt 404-bugfix)

A 2026-07-28-ról függő belépett vizuál-QA elvégezve. Persona-seed
helyreállítva (`pnpm seed:personas`, 48 user a Clerk dev instance-ban),
QA-fixture: 3 persona szerepekkel az „Alma és Társa Kft."-ben
(thor-inte=ORG_ADMIN, open-adap=ORG_MANAGER, open-thor=ORG_MEMBER).

- **Admin renderel** ✅ — mind a szekció él (részvétel, önkép/külső kép,
  csapatszerep, kapcsolati beágyazottság, kapott visszajelzések),
  forrás-badge-ek és min-2/min-3 kapuk rendben.
- **Manager redirect** ✅ (szervezet-oldalra), **tag redirect** ✅
  (/profile/results-re).
- **leftAt → 404**: eredetileg ❌ — client-side Application error. Ok: a
  root not-found `redirect("/")`-je streamelt `notFound()`-nál kliens-
  kivételként csapódik le (teljes útvonal-mismatchnél 307-tel működik).
  Fix: `src/app/(app)/not-found.tsx` — barátságos, lokalizált 404-oldal.
  Újratesztelve ✅, leftAt visszaállítva.

## Újrafuttatási körök — pilot előtti tételek (terv 1.1 + 1.2) KÉSZ

- **1.1 — append-only csapatszerep-körök** (az egyetlen adatvesztéses
  pont zárva): TeamRoleAnswer/TeamRoleScore `@unique(userProfileId)`
  feloldva, `campaignId?` kör-címke (Campaign FK, SetNull) +
  `(userProfileId, createdAt)` index. Submit route upsert→create;
  `resolveActiveCampaignIdForStep` (campaign-steps.ts) címkézi a beadást
  a nyitott TEAM_ROLE lépésű aktív kampányra. Olvasók legfrissebbre
  állítva: results page, team-stats, TeamRoleTabView, role-round route.
  seed-banan-demo idempotens append-re írva. Migráció kézzel (shadow-DB
  bug): `20260729093406_team_role_append_only_rounds`.
- **1.2 — újrafelvételi kör**: `Campaign.requireFreshResults` +
  `activatedAt` (első ACTIVE-váltáskor rögzül);
  `initializeCampaignProgress` fresh körben csak aktiválás utáni
  self-eredményt fast-forwardol. Wizard 2. lépés checkbox (HU+EN),
  kampány-kártya „Újrafelvételi kör" chip. Migráció:
  `20260729094220_campaign_fresh_results`. A retake-út él
  (/assessment?confirmed=true), az AssessmentResult append-only.

## Interakció-szimuláció F1 — reláció-atom content-készlet KÉSZ

`src/lib/interaction-atoms.ts` (ütemterv szerinti júl 27–31 sáv):

- 18 azonos-dimenziós atom (6 dim × high-high/high-low/low-low) + 12
  kereszt-atom a FRICTION_WEIGHTS top-jóslói mentén (THOR×ADAP,
  OPEN×THOR, TEMP×RESO, INTE×ADAP…), HU+EN, easy/friction/discuss
  blokkokkal; aszimmetrikus atomoknál mindkét nézőpont (view/viewB).
- 6×2 vezető-kiegészítő (LEADER_SUPPLEMENTS) a vezető-módhoz.
- `findAtom`/`atomBlocksFor` irányfüggetlen lekérdezők — az F2 motor
  belépési pontjai.
- Guardrail-teszt (tests/unit/platform/interaction-atoms.test.ts):
  lefedettség, HU+EN kitöltöttség, egyedi pólus-párok, tükrözés.

Következő: F2 motor (`interaction-engine.ts`, aug 3–4).

## TSFI EN-itemek IPIP-eredete — B1 nyitott szál LEZÁRVA

Mind a 100 EN item gépi összevetése az IPIP hivatalos kulcs-oldalaival:
95/100 szó szerinti egyezés (E/1 alany hozzáadásával), 5 dokumentált
könnyű adaptáció (12/36 „would"-betoldás újonnan listázva; 41/50/78 már
ismert). HEXACO-PI-R védett szöveggel nincs egyezés. Provenance-doksi
új „Szövegszintű ellenőrzés" szakasz; audit-doksi + CLAUDE.md frissítve.

## Lint-adósság — feloldva (config-fix volt)

A „~60 örökölt lint-hiba" valójában a `.claude/worktrees/` alatt maradt
agent-worktree build-artifactjaiból jött (a lint 830 fantom-hibát
szkennelt). `.claude/**` a globalIgnores-ba került — valós állapot:
**0 error, 41 warning** (no-unused-vars jellegű; boy-scout-rule-lal
fogyasztható). A worktree-t (claude/adoring-lumiere-f37f17, uncommitted
nav-módosításokkal) NEM töröltem — döntést igényel.

## Resend from-domain ops (P8) — állapot

DNS friss ellenőrzés (2026-07-29): trita.io MX/SPF/DKIM kint ✅,
**DMARC továbbra is hiányzik** ❌. Vercel CLI nincs belépve — a maradék
lépés user-oldali (resend-domain-ops.md 1–4): Resend dashboard verify,
`RESEND_FROM_EMAIL` prod env, `_dmarc.trita.io` TXT felvétele
Cloudflare-ben, éles teszt-küldés.

## Verifikáció

tsc 0 hiba · lint 0 error/41 warning · unit 285/285 · client 59/59.
Commitok: 6aa40ae (404-fix), 59e952f (lint-ignore), 5f59680 (1.1),
6784b9f (1.2), 2690996 (F1), cbbf921 (provenance).

## Logging-rendszer — strukturált logger + request-korreláció (esti kör)

- **Új logger-stack**: `src/lib/logger.ts` (zéró-dep, strukturált JSON
  prod / színezett dev, LOG_LEVEL/LOG_JSON, kulcs-alapú redaction,
  err-szerializálás, child bindingok) + `logger.server.ts`
  (getRequestLogger: requestId+path binding a headers()-ből) +
  `client-logger.ts` (kliens; prodban csak warn/error).
- **Request-ID**: proxy.ts minden kérésre x-request-id-t generál/továbbad
  (request+response fejléc, x-pathname a request-fejlécekre) —
  egy kérés minden logsora korrelálható; `instrumentation.ts`
  onRequestError a kezeletlen hibákra.
- **Migráció**: mind a 84 console.* hívás (35 fájl) az egységes loggerre
  állt, event-név konvencióval (domain.action) és mezőbe tett
  azonosítókkal. Clerk webhook nyers payload-log (OTP-veszély!) debug
  szintre + csak kulcs-listára szelídítve.
- **Lint-guard**: no-console = error az src alatt.
- Konvenciók: docs/development/logging.md. Verifikáció: tsc 0, lint 0
  error, unit 291/291 (+7 logger-teszt), client 59/59; élő smoke:
  invalid submit → WARN sor a válasz-fejléccel egyező requestId-vel.

## HEXACO visszaállás — a TRITAN-branding kivezetve (éjszakai kör)

User-döntés: a TRITAN erőltetett, az IPIP–HEXACO eredet miatt a HEXACO a
hiteles keret. **Display-only csere** (a belső TEMP/RESO/… kódok és a
DB score-JSON-ok NEM változtak — nincs adat-migráció):

- `src/lib/tritan.ts` kanonikus térkép: H·E·X·A·C·O betűk, szakirodalmi
  nevek (Becsületesség-Alázat, Emocionalitás, Extraverzió, Barátságosság,
  Lelkiismeretesség, Nyitottság), HEXACO-PI-R facet-nevek
  (Mohóságkerülés, Szorongás, Társas merészség, Szorgalom…), kanonikus
  sorrend H,E,X,A,C,O; altruizmus-skála „Altruizmus" néven.
- Hardcode-olt címke-térképek átírva: team-stats, OrgOverviewTab,
  profile-content, team-report-member, DynamicsMap, TeamMap,
  TeamReportView + i18n (landing/org/results radar-legend).
- Kérdésbank (questions/tritan.ts): dimenzió- és facet-címkék + mind a
  6+1 leírás HU+EN HEXACO-terminológiával; teszt-leírás „HEXACO-alapú,
  validált személyiségfelmérés (IPIP-itemek)".
- Sorrend-hardcode-ok a kanonikus TRITAN_ORDER-re kötve: RadarChart,
  ProfileTabs akkordeon + áttekintő (radar mostantól H·E·X·A·C·O-t
  olvas ki fentről).
- Látható TRITAN-szövegek cserélve (admin overview/feedback,
  TeamRoleSection „profil-alapú becslés"); help-topic + kérdésbank
  nevesíti a HEXACO-t és az IPIP-et. TSFI kérdőív-név MARADT.
- CLAUDE.md szabály-flip: HEXACO nevesítése mostantól KÍVÁNATOS;
  tritan-naming.md történeti dokumentummá minősítve.
- Böngésző-verifikáció: results-oldal hero + radar (H E X A C O) +
  facet-bontás élőben ellenőrizve. tsc 0 · lint 0 error · unit 291/291
  (member-dossier sorrend-teszt igazítva) · client 59/59.

### HEXACO — 2. kör (riportok + admin + narratívák)

Kisbetűs/ragozott régi nevek és rejtett előfordulások: team-report
kör-narratíva dim-nevei, CareerCompass, profile-content karrier-szövegek
(HU+EN), team-role-scoring indoklások, team-report-member tag-tippek,
TeamReportView súrlódás-magyarázó, workstyle EN, interaction-atoms.
Admin: a draft-emlékeztető tábla nyers `TRITAN` testType-ja „HEXACO
(TSFI)" címkével jelenik meg. Publikált riport-aggregátumok ellenőrizve:
kódokat tárolnak, renderkor már HEXACO-nevek — nincs adat-teendő.

### HEXACO — 3. kör (nyers dimenziókód-badge-ek)

A csapat-személyiségprofil és társai a NYERS belső kódot (TEMP/RESO/INTE/
THOR/ADAP/OPEN) renderelték a színes badge-ekben — ezek voltak a
„beragadt tritán jelölések". Mind HEXACO-betűre (H/E/X/A/C/O) állítva
hexLetter() helperrel: manager cockpit top-3 chip, TeamHeatmap
fejléc+magyarázó, TeamInsights, DimensionDetail, FacetBreakdown,
BlindSpotAnalysis. Dossié élőben ellenőrizve.

### HEXACO — 4. kör (blog + kliens-hibalog UX)

- **Blog**: a két TRITAN vs MBTI cikk (HU+EN) teljes tartalma HEXACO-ra
  írva (cím, tagek, CompareTable, DimBadge-címkék, módszertani jegyzet
  IPIP-említéssel); tag-színlisták „tritan"→„hexaco". A slug/URL maradt
  (tritan-vs-mbti) — átnevezés redirect nélkül linktörés lenne.
- **Kliens-logger**: a Next dev-overlay nem kapja el többé a kezelt
  hibákat — error-szint dev-ben warn-sinkre megy (prodban marad
  console.error), és az err mező szerializálódik (Error/Clerk errors[]
  → name/message; eddig üres {} látszott). A kezelt kliens-hibák
  (sign-in/up, submit-hibák, invite, claim, profil-törlés) szemantikailag
  is warn-ra álltak (UI úgyis jelzi őket).

### FE-hibajavítások (blog hydration + sign-up kódnézet)

- **Blog hydration mismatch**: a statikus blog-lista SSR-HTML-je a default
  nyelvvel készül, de a LocaleProvider localStorage-effektje már hydration
  KÖZBEN átválthatta a nyelvet — a később hydrálódó lista-részfa így más
  posztokat (és más generatív SVG-ket) renderelt, mint a szerver-HTML.
  Fix: displayLocale two-pass (első render mindig default, mount után vált).
- **Sign-up kódbeviteli nézet szétcsúszás**: a #clerk-captcha div a
  flex-SOR gyereke volt `hidden`-nel — a Clerk odamountolta a Turnstile-t
  és beékelődött a bal panel és az űrlap közé. A div az űrlap-oszlopba
  került (a sign-in mintájára).
- Stale Prisma-kliens a futó dev serverben (org-oldal
  requireFreshResults-hibák) — server-restarttal rendezve; a migráció
  utáni `prisma generate`-et a Turbopack-cache nem vette fel.

## Karrier-modul felújítás — mind a 11 javaslat (éjszakai 2. kör)

Pontosság:
- **Facet-szintű súlyok** (FACET_REFINEMENTS, 26 kulcs-szerep): a szerepet
  ténylegesen jósló facetek számítanak a dim-átlag helyett (Fejlesztő:
  perfekcionizmus+körültekintés+kíváncsiság; Értékesítő: társas merészség+
  élénkség...); „facet-pontosított" badge. Fallback dim-súlyokra.
- **Altruizmus bekötve** a segítő szerepeknél (care/therapy/clinical/
  nonprofit/specialed/mentor).
- **Konfidencia-sáv**: computeFitBand — rövid forma ±8, teljes ±5,
  observer-megerősítéssel szűkebb; sáv-vizualizáció a fit-báron, a
  pontszám helyett tartomány (assessmentForm a questionCountból).
- **Observer-blend badge**: „külső képpel megerősítve" + szűkebb sáv.
- **RIASEC-light**: szerep-Holland-kódok (iparági default + 36 override),
  becsült user-kód (top-2 betű, explicit „becslés" kerettel), ±3
  döntetlen-bontó a rangsorban.
Profizmus:
- **Módszertani panel** („Mire épül ez a becslés?") forráshivatkozással
  (Ashton&Lee 2007, Barrick&Mount 1991, Sackett 2022, Holland 1997).
- **PDF karrier-oldal** (CareerPage): top-3 irány sávval+tierrel,
  fejlődési fókusz, módszertani keret — csak kitöltött wizard után.
Élmény:
- **Reveal-hero**: ProgressRing a top iránynak, hero-mondat facet-pontos
  komponensekből.
- **Szerep-összevetés**: két kártya kiválasztható, komponensenkénti
  side-by-side bontás.
- **30 napos mini-terv**: a fő fejlesztendő dimenzió GrowthPlan-jéből
  (viselkedés/reflexió/kihívás).
- **Vezetési közeg teaser**: iparág→tipikus vezető-pólus térkép +
  LEADER_SUPPLEMENTS szöveg (interakció-F1 újrahasznosítva).
Verifikáció: 7 új unit teszt (298/298), teljes wizard-flow élőben
végigvive persona-userrel — RIASEC-kód, sávok, badge-ek, összevetés,
mini-terv, vezető-blokk mind renderel. tsc 0 · lint 0 error · client 59/59.

## Karrier-iránytű 2. ütem — katalógus-bővítés + pontosító kérdések + élmény

- **Katalógus**: 3 új iparág (Szakmák/ipar · Közlekedés/szállítmányozás ·
  Személyi szolgáltatás/wellness, 17 szerep) + 13 új szerep a meglévőkbe
  (IT-biztonság, rendszergazda, műszaki író, pszichológus, orvos,
  gyógyszerész, védőnő, nyelvtanár, ped. asszisztens, adótanácsadó,
  biztosítás, szociális munkás, környezetvédelem) — összesen ~95 szerep /
  16 iparág. RIASEC/vezető-kontextus/edu-affinitás frissítve.
- **Végzettség-jegyzet**: minden szerep EduReq címkét kapott (open/course/
  vocational/higher/specialized) — a kártyán „🎓 Belépés: …" megjegyzés.
- **Új pontosító kérdések**: 3 munkakörnyezet-tengely (tempó / keretek /
  helyszín) — szerep-oldali értékek iparági default + felülbírálás
  (roleEnvProfile, init rárakja a role.env-re), scorePrefMatch a 7
  tengelyt együtt méri; + vezetői kérdés önálló lépésként (leadFocus a
  wizardból állítódik, chip-válasszal).
- **Élmény**: 8 lépéses flow százalékos progress-sávval, lépés-átmenet
  animáció, auto-advance az egyválasztós lépéseken.
- Verifikáció: 2 új guardrail-teszt (300/300 unit), teljes wizard élőben
  végigvive — env-kérdések átrendezik a rangsort, edu-jegyzet renderel,
  új iparágak a választókban. tsc 0 · lint 0 error · client 59/59.

### Karrier-wizard élmény-kör + kijelölés-glitch fix

- **Glitch-ok megtalálva**: (1) az auto-advance lépések kijelölés-állapota
  sosem látszott — a kattintás szinkronban léptetett, visszalépéskor pedig
  „ugrott" a vizuál; (2) az inline `fadeIn` animáció-név nem létezett a
  globals.css-ben (fade-in a valódi), így minden lépés-animáció némán
  no-op volt. Fix: selectAndAdvance (látható kijelölés + 320ms késleltetett
  léptetés, dupla-katt guard) + valódi keyframe-ek (cc-step-in, cc-pop,
  reduced-motion kikapcsolással).
- **UI candy**: OptionCard komponens az egyválasztós lépéseknek (emoji +
  címke + alcím, pop-animáció): állapot- és vezetői kérdés; emoji a
  terület- és iparág-chipeken (16 iparág + 10 képzési terület); prefs/env
  sorok kapszula-szegmens konténerben; kijelölés-pop minden chipen;
  CelebrationBurst a wizard befejezésekor.
- Vezetői kérdés kijelölése most stateful (leadChoice) — a korábbi
  active-logika csak az „Igen" opciót tudta jelölni.
- Élő verifikáció: kattintás → pop → 320ms → következő lépés; teljes
  8 lépéses flow végigvive, eredmény + RIASEC renderel.

## Karrier 3. ütem — edu-kapuzás, több terület, érdeklődés-címkék, MÉRT Holland-kód

- **Edu-kapuzás**: általános/érettségi szintnél a terület-választó el sem
  jelenik (animált megjelenés vocational/higher-nél), eduFields reset.
- **Több terület**: eduFields[] (max 3), boost = affinitás-listák uniója;
  örökölt eduField-del visszafelé kompatibilis (normalizedEduFields).
- **Érdeklődés-címkék**: 12 emoji-címke (INTEREST_TAGS, max 4) új wizard-
  lépésként (9 lépés összesen); RIASEC-betű mapping + iparág-affinitás
  boost (+4, tagBoosted flag).
- **MÉRT Holland-kód**: 30 itemes érdeklődés-kérdőív (questions/riasec.ts,
  O*NET Mini-IP mintájára, public domain forrás, HU+EN, betűnként 5 item,
  1-5 skála → 0-100/betű). RiasecProfiler komponens az eredmény-nézetben
  (CTA-kártya → egy item/képernyő, auto-advance, progress) — a wizard
  vizuális nyelvén. Mentés a careerBackground.riasecScores-ba.
- **Megbízhatósági lépcső** (resolveUserRiasec): mért kérdőív > címkék
  (személyiség tie-break 0.3) > személyiség-becslés; a UI forrás szerint
  címkéz (mért/címkés/becsült). Mért kódnál az érdeklődés-egyezés valódi
  komponens a rangsorban (55% személyiség + 20% preferencia + 25%
  érdeklődés), a ±3 tie-break kikapcsol.
- 5 új unit teszt (305/305). Élő verifikáció: edu-kapuzás, multi-terület,
  címke-lépés + számláló, címke-alapú kód, profiler-flow (tap→advance),
  mért végállapot (Mért érdeklődés-kódod badge + átrendezett rangsor).
- Redundancia-értékelés: a három szint NEM redundáns — címke = 30 mp-es
  gyors jel a wizardban, Mini-IP = opt-in pontosítás, becslés = fallback.

### Holland-kód értelmező oldal (/holland-kod)

Publikus, SEO-indexelt magyarázó oldal a marketing-fában: a hat RIASEC-
betű színkódolt kártyákon (név, tagline, leírás, jellemző tevékenységek,
példa-szerepek a saját katalógusból), „hogyan olvasd a kódodat" szekció
kombináció-példákkal, forrás-jegyzet (Holland 1997, O*NET public domain),
CTA a Karrier-iránytűre. Közös tartalom-modul: src/lib/riasec-content.ts.
A karrier-modulból linkelve: „Mit jelentenek a betűk?" a kód-sorból + a
kártyák Holland-kód chipjei is kattinthatók (új fülön nyílik).

## Notification: kattintás = olvasott (bugfix-csomag)

Az olvasatlan értesítésre kattintás navigált, de az elem olvasatlan
maradt. Három rétegű javítás:
1. Hiányzó egyedi mark-read: a panel-elem kattintása csak a panelt zárta —
   új markRead(id) a providerben (optimista) + POST ids:[id] (az API már
   tudta).
2. Badge-verseny: a navigáció SSR-je hamarabb olvasta a countot, mint a
   mark-read commitolt — a panel-link most preventDefault + await
   markRead UTÁN router.push.
3. Rejtett state-bug (a dismiss-ben is): a wasUnread flag a setItems
   funkcionális updater-én BELÜL számolódott, ami nem garantáltan fut
   szinkron — a badge-csökkentés így kimaradhatott. A flag most az
   aktuális items-ből számolódik az updater előtt (markRead + dismiss).
Élő verifikáció: kattintás → badge eltűnik, unread-count API 0, navigáció
a notif linkjére.

## PDF-riport — tördelés-fix + site-hű design (raszter-alapú iterációval)

Munkamódszer: pnpm report:personas egy personára + pdftoppm raszter →
oldalankénti vizuális ellenőrzés, javítás, újragenerálás (4 kör).

- **Üres/lebegő oldalak GYÖKÉR-OKA**: a page-body konténerek flex:1 +
  KONTÉNER-szintű bottom-padding — a react-pdf a lap-paddinget kezeli jól
  töréskor, a konténerét túlcsordulásként (láthatatlan folytatás-oldal,
  csak fix fejléc/lábléc). Fix: flex:1 ki, bottom-padding a Page-stílusra.
- **Tördelés-kiegyensúlyozás**: az „Ahogy működsz" kártya a félig üres
  összefoglaló-oldalra került (a munkastílus-oldal rövidült); a start-
  oldal „Dimenziók részletesen" előnézete plus-riportban kikapcsolva
  (a facets-oldal teljes bontást ad — redundáns volt, és a csapatszerep-
  kártyát lökte üres oldalra); facets-sortávok szűkítve, hogy a záró
  összegzés-kártya ne árválkodjon külön oldalon.
- **Tipográfia**: globális betűméret-emelés egy fokozattal (5→5.5 …
  7.5→8; a ui-unification nyitott „PDF 8→9.5pt" tétele), archetípus-
  történet bronz pull-quote-ként (a felület idiómája szerint).
- **Radar**: 150pt, a felület egybetűs HEXACO-tengelyjelei (H·E·X·A·C·O)
  Fraunces 7.5pt-ben.
- Eredmény: plus-riport 6 teli oldal, nulla üres/fél oldal; start-riport
  2 tartalmas oldal. tsc 0 · lint 0 error · 305/305 unit · 59/59 client.

## Csapatba-kerülés értesítés + Karrier-fül org-tagoknak

- **Hiányzó notification bekötve**: a TEAM_MEMBER_ADDED típus + i18n
  létezett, de handler és hívás SOHA nem készült hozzá — meglévő user
  csapatba felvételekor semmi jelzés nem ment. Új handleTeamMemberAdded
  (orchestrator, dedupe-kulccsal), bekötve MINDKÉT hozzáadási útra
  (members route = picker, invite route = meglévő user email-lel).
  A szöveg átírva a FELVETT usernek címezve, és kimondja a lényeget:
  „Az eddigi eredményeid megmaradnak — nem kell újra kitöltened a
  felmérést." Link a csapat-oldalra.
- **Karrier-fül vissza mindenkinek**: org/team tagoknál a fül rejtve
  volt („konzultációs fókusz"), de a kitöltött karrier-adat így elveszett
  a felületről, miközben a PDF-be továbbra is bekerült — inkonzisztens.
  A kapuzás megszűnt.
- Élő verifikáció: handler-szimuláció (notif létrejön, dedupe 1 db) +
  harang-panel renderel + Karrier-fül látszik org-tag personának.

## Org-kapcsoló: karrier-modul elrejtése (trita admin)

Új org-szintű kapcsoló, amit KIZÁRÓLAG a trita admin állít
(`/admin?tab=orgs`): bekapcsolva az org tagjainál eltűnik a Karrier-
iránytű fül a `/profile/results`-ról ÉS a PDF-riport karrier-blokkja
sem generálódik. Alapértelmezés: KI (karrier mindenkinek látszik, a
4fb1bb9-es visszaállítás szerint).

- **Séma**: `Organization.hideCareerModule Boolean @default(false)` +
  kézi migráció (`20260729125431_add_org_hide_career_module`, db execute
  + migrate resolve — shadow DB továbbra is törött).
- **API**: `POST /api/admin/org-access` új `set_career_module` akció
  (`hideCareerModule: boolean`), requireAdmin-guard mögött; GET-be és
  OrgsTab-lekérdezésbe bekerült a mező.
- **Admin UI**: AdminOrgAccessSection új „Modulok" sor org-kártyánként —
  checkbox „Karrier-iránytű elrejtése a tagoknak (fül + PDF-blokk)" +
  „rejtve" badge.
- **Kliens-kapu**: results page a `organizationMember.findFirst(leftAt:
  null, org.hideCareerModule: true)` lekérdezéssel dönt — BÁRMELY aktív
  org-tagság rejtése elég (szigorúbb szabály nyer). `careerModuleHidden`
  prop a ProfileTabs-nak: fül-szűrés + a PDF `career` objektum nem épül;
  `?tab=career` URL results-ra esik vissza.
- **Ellenőrzés**: élőben persona-open-thor ideiglenes org-tagsággal —
  flag BE: csak Eredmények/Összehasonlítás fül; flag KI: Karrier fül
  vissza. tsc 0, lint 0 error/41 warning, 305 unit + 59 client zöld.
  Ideiglenes tagság + teszt-script eltávolítva.

## Kampány-statok lépés-tudatosra + fresh-szűrés (245cc9d)

A kampány-felület eddig MINDIG a személyiség-teszt (self/observer)
kitöltöttségét mutatta — akkor is, ha a kampány szerep-kört, bizalmi
kört vagy pulse-t mért, és fresh-körnél a régi eredmények miatt azonnal
„késznek" látszott mindenki. Mostantól:

- **Közös helper**: `isCampaignStepDone`/`countCampaignStepsDone`
  (campaign-steps-core) — lépés kész, ha currentStep túlhaladt VAGY
  stepCompletions bejegyzés van VAGY (legacy OBSERVER_360 fallback)
  fresh-tudatos self-eredmény. 9 új unit teszt (campaign-steps.test.ts).
- **Részletező** (`/org/[id]/campaigns/[campaignId]`): lépés-haladás
  szekció minden kampánynál (nem csak több-lépésesnél); self/observer
  stat-kártyák CSAK OBSERVER_360-lépéses kampánynál; fejléc-% = minden
  lépéssel végzett résztvevők; résztvevő-sorok „x/y lépés · most: <lépés>"
  chipekkel; „Újrafelvételi kör" badge; requireFreshResults-nál a
  self/observer lekérdezések aktiválás utánra szűrve.
- **org-stats.ts / CampaignCard / OrgOverviewTab**: CampaignWithStats
  bővítve (steps, stepProgress, fullyDoneCount, hasObserverStep,
  résztvevőnként stepsDone/doneAll, fresh-tudatos selfDone/observerCount);
  aktív kártya szegmentált sávja + lenyíló bontása a kampány SAJÁT
  lépéseit mutatja; lezárt kártya %-a lépés-alapú; overview-banner
  „minden lépéssel végzett" számot mutat.
- **Remind route**: „nem kezdte" = a kampány lépéseiből semmit sem
  teljesítők (nem a self-teszt hiánya).
- Ellenőrzés élőben (ideiglenes ORG_CONSULTant-nézettel): TRUST_360-only
  kampány 2/8 lépés-kitöltéssel + 25% fejléc; 3-lépéses kampány helyes
  lépés-sorai + observer-kártyák. tsc 0, lint 0 error, 314 unit + 59 client.

## Draft mérés elvetése

- `DELETE /api/org/[id]/campaigns/[campaignId]` — CSAK DRAFT státuszban
  (409 CAMPAIGN_NOT_DRAFT egyébként), tanácsadói + capability guard;
  cascade a résztvevő-rekordokra (beadott adat vázlatnál nincs).
- DraftCampaignEditor: „Vázlat elvetése" gomb (confirm + rose-stílus,
  elválasztva a mentéstől), sikeres törlés után vissza a Mérések fülre.
  i18n: org.campaign.discard* kulcsok (HU/EN).
- Élőben ellenőrizve teszt-vázlaton: gomb → confirm → törlés → redirect.

## Kampány-bővítések: több csapat, típus-limit el, rész-haladás, tag-nézet

- **Több cél-csapat**: `Campaign.teamIds String[]` (migráció:
  `20260729132948_add_campaign_team_ids`, backfill a legacy teamId-ból;
  teamId = első csapat, riport-híd kompatibilitás). Wizard + draft-szerkesztő
  checkboxos többes választás (résztvevők = csapatok uniója); create/edit API
  minden csapatot org-hoz validál; TEAM_ROLE aktiválás/zárás minden
  kampány-csapaton (updateMany). Kitöltő-oldalak és submit API-k (trust,
  team-role-360, peer-feedback) a tag SAJÁT csapatát oldják fel
  (`resolveCampaignTeamIdForUser`); lezárt kampány riport-hídja csapatonként.
- **Nincs típus-limit**: max 4 → mind a 6 mérés mehet egy körben
  (create + edit_draft séma, wizard/editor UI limit-logika törölve).
- **Rész-haladás mentve + mutatva**: PEER_FEEDBACK submit lefedettség-alapú
  léptetésre állt (a team-role-360 mintája) — a kliens személyenként is
  beküld (PeerFeedbackClient: kész kártya továbblépéskor azonnal szerverre
  megy, localStorage fallback marad); a lépés csak akkor záródik, ha minden
  csapattárs megkapta a feedforwardot. Manager-részletezőn a résztvevő-sor
  „most: <lépés> · 2/5" rész-számlálót és „Elkezdte" chipet mutat
  (TEAM_ROLE_360/TRUST_360/PEER_FEEDBACK).
- **Tag-oldali kampány-nézet**: új `/assessment/measurements` oldal — saját
  aktív mérések lépés-listával (kész ✓ / nyitott CTA / ütemezett „Várható
  nyitás" / hátralévő), lépésen belüli rész-haladással („1/2 értékelés
  kész") és vissza-linkkel a kitöltőkre; link a team-oldali mérés-bannerből
  („Összes mérési feladatom →"). i18n: `myTasks.*` (HU/EN).
- Élő E2E (ideiglenes teszt-csapat + aktív kör, utána törölve): wizard mind
  a 6 méréssel + 2 csapattal draftot hozott létre (teamIds ✓, 10 fős unió ✓);
  trust rész-beadás → „1/2 értékelés kész" + manager „· 1/2"; második beadás
  → covered, lépés kész, pulse ütemezett nyitással. tsc 0, lint 0 error,
  314 unit + 59 client zöld.

## Kezdés→Folytatás CTA megkezdett lépésnél

- Közös helperek (campaign-steps.ts): `getStepPartialProgress` (értékelős
  lépések rész-számai) + `hasStartedStep` (rész-beadás VAGY OBSERVER_360-nál
  szerver-oldali felmérés-piszkozat) — a tag-nézet erre állt át.
- Team-oldali mérés-banner: `pendingMeasurement.started` flag — a CTA
  „Kitöltöm most"/„Visszajelzést adok" helyett „Folytatom a kitöltést".
- Bugfix mellékesen: a team-oldali banner kampány-szűrése a több-csapatos
  kampányokat is látja (OR: teamId / teamIds has) — enélkül a 2. csapat
  tagjai nem kapták volna meg a felhívást.
- Élőben: teamId=null + teamIds=[csapat] kampányon rész-beadás előtt
  „Kitöltöm most", utána „Folytatom a kitöltést" (banner) és „Kitöltés
  folytatása" (tag-nézet). tsc 0, 314 unit + 59 client, lint 0 error.

## Kampány-törlés bármely státuszban

- A DELETE végpont DRAFT-guardja feloldva: a tanácsadó/admin AKTÍV és
  LEZÁRT mérést is törölhet — kitöltésekkel együtt. A kör-adatok
  (résztvevők, trust/szerep-megfigyelések, peer-visszajelzés, pulse)
  cascade-del törlődnek; a userek SAJÁT eredményei (self teszt,
  szerep-kérdőív — SetNull) megmaradnak. Futó TEAM_ROLE-körnél a
  csapat(ok) teamRoleRoundActive flagje resetel.
- Új CampaignDeleteButton (inline kétfázisú megerősítés, natív dialog
  nélkül; a szöveg explicit: a beadott értékelések is törlődnek) — a
  részletezőn ACTIVE-nál a Mérés kezelése kártyán, CLOSED-nál külön
  kártyán. A vázlat-elvetés a szerkesztőben maradt (ugyanez a végpont).
- Törölt mérés sehol — a lezárt körök listájában sem — jelenik meg.
- Élőben: aktív kör beadott trust-értékeléssel → törlés → kampány + kör-
  adatok eltűntek DB-ből, lista tiszta. tsc 0, 314 unit + 59 client,
  lint 0 error.

## Observer-kampány: meghívó-felület a tagoknak + notif-link fix

Bug: futó külső visszajelzési körben (OBSERVER_360) az org-tag a Külső
kép fülön csak a „folyamatban" állapot-kártyát látta — meghívó-kezelő
NÉLKÜL, pedig a külső adat kampányban is a tag saját meghívóiból érkezik
(nincs automatikus kolléga-kijelölés). Így senki nem tudott értékelőt
felkérni.

- **ProfileTabs Külső kép fül**: `in_progress` állapotban az állapot-
  kártya ALATT megjelenik az InvitationsTab (kolléga-picker a szervezet
  tagjaiból + külső e-mail meghívó + link-megosztás); `available`
  állapotban az összevetés mellett is. Csak a `locked` (nincs kampány)
  marad kártya-only.
- **Kártya-szöveg**: a passzív „kollégáid most adnak rólad visszajelzést"
  helyett cselekvésre hívó: „az alábbi meghívó-kezelőből küldj felkérést
  kollégáknak vagy külső partnereknek".
- **Küszöb-konzisztencia**: InvitationsTab `minForReveal` prop —
  kampány-módban az info-banner is 3-at ír (nem 2-t);
  `invitations.infoNeededN` i18n kulcs.
- **Notif-linkek**: MEASUREMENT_STEP_OPENED OBSERVER_360-nál kész self
  esetén `/profile/results?tab=comparison`-re visz (nem a kitöltőre);
  CAMPAIGN_LAUNCHED linkje szerep-tudatos — ORG_MEMBER a saját
  feladat-nézetére (`/assessment/measurements`), manager/tanácsadó a
  kampány-fülre kerül (eddig a tag a tanácsadói oldalról redirectet kapott).
- Élőben: aktív observer-körrel a fülön kártya + kolléga-lista
  („Meghívom") + külső meghívó látszik, 3-as küszöb-szöveggel.
  tsc 0, 314 unit + 59 client, lint 0 error.

## Belsős observer-meghívó: címzett-kapu + kolléga-előtöltés + teendő-lista

- **Csak a címzett töltheti ki**: név szerinti (observerProfileId-s) belsős
  meghívónál a /observe/[token] bejelentkezést kér (redirect_url-lel vissza),
  és más bejelentkezett usernek „Ez a meghívó nem neked szól" kártyát mutat.
  Szerver-oldali guard a draft- ÉS submit-API-n is (403 NOT_ADDRESSEE).
  Külsős (e-mail/link) meghívó marad publikus — ott ilyen validáció nem
  lehetséges.
- **Kapcsolat előtöltve**: belsős meghívónál a kezdő kapcsolat-kérdés
  „Kolléga"-ra állítva, a többi opció szürke/disabled, magyarázó jegyzettel
  (observer.relationLockedNote); a localStorage-piszkozat sem írhatja felül.
- **Teendő-lista**: a felkért tag a /assessment/measurements oldalon
  „Visszajelzés-kérések" szekcióban látja, ki kér tőle visszajelzést —
  megkezdett kitöltésnél „x/y kérdés kész" badge (szerver-oldali
  ObserverDraft) és „Kitöltés folytatása" CTA.
- Élőben igazolva: teendő-sor + előtöltött/zárolt kapcsolat-opciók + idegen
  címzett 🔒-kártya + draft-API 403. tsc 0, 314 unit + 59 client, lint 0 error.

## Demo-szervezet seed (scripts/seed-demo-org.ts)

„Horizont Kft." + „Horizont csapat": 5 bejelentkezhető Clerk teszt-user
(horizont-*+clerk_test@trita.io, kód 424242), kitöltött TRITAN self-profillal
(5 elütő HEXACO-mintázat), observer-adat nélkül; Anna ORG_ADMIN, aktív
team-előfizetés (12 hó), a platform-admin ORG_CONSULTANT-ként hozzárendelve
(saját fiókból indítható mérés). Idempotens; `--teardown` mindent töröl
(Clerk-userek maradnak, újrahasznosulnak).

## Tag-nézet: observer-kör gyűjtés-haladás

Bug: self-kész tagnál az OBSERVER_360 lépés fast-forwarddal azonnal
„kész"-re állt, és a tag-nézet semmit sem mutatott a külső visszajelzés
gyűjtéséről — a tag nem látta, hogy meghívókat kell küldenie (min. 3),
sem a beérkezést.

- /assessment/measurements: OBSERVER_360-lépéses kampánynál a kártya a
  saját meghívó-állapotot számolja (küldött meghívók + beérkezett
  visszajelzések, fresh-tudatosan, küszöb: OBSERVER_MIN_FOR_REVEAL=3).
- A lépés-sor a self-kész után „0/3 visszajelzés beérkezett · n meghívó"
  badge-et és CTA-t mutat: „Kérj visszajelzést (min. 3)" (ha még nincs 3
  meghívó) / „Meghívók kezelése" — a Külső kép fül meghívó-kezelőjére visz.
- A kártya-fejléc „Minden lépés kész" helyett „Külső visszajelzés gyűjtés
  alatt" badge-et kap, amíg a küszöb nincs meg.
- A NEKI címzett kérések (amikre válaszolnia kell) a meglévő
  „Visszajelzés-kérések" szekcióban jelennek meg (338c579), kérdés-szintű
  haladással.
- Élőben: self-kész taggal a kártya „gyűjtés alatt" + 0/3 + CTA. tsc 0,
  314 unit + 59 client, lint 0 error.

## Csapat-nézeti observer-gyűjtés banner + nyíl le a feladat-linkről

- A team-oldali „Összes mérési feladatom" link nyíl nélkül.
- Futó observer-körnél a csapat-áttekintésen is megjelenik a gyűjtés
  állapota (self-kész fast-forward után is): „Külső visszajelzés —
  gyűjtés alatt" kártya a kampány nevével, „n/3 beérkezett · k meghívó
  elküldve" számlálóval és CTA-val („Kérek visszajelzést" / „Meghívók
  kezelése" → Külső kép fül) — a teendő nem vész el az eredmény-oldalon.
  Fresh-tudatos számítás, küszöb: OBSERVER_MIN_FOR_REVEAL. Adat:
  team/[id]/page.tsx observerGathering → ctx (types.ts) → OverviewTabView.
- Élőben: teszt-csapaton a banner 0/3 + CTA-val renderel. tsc 0, 314 unit
  + 59 client, lint 0 error.

## Beérkező observer-kérések a csapat-nézeten + nyíl-mentes Kitöltöm

- `invitations.fillIn` kulcs nyíl nélkül („Kitöltöm") — a Külső kép fül
  beérkező-meghívó listájának gombja.
- Csapat-áttekintésen új „Tőled kért visszajelzés" blokk: a csapattársaktól
  érkezett függő observer-meghívók (ki kérte), megkezdett kitöltésnél
  „12/60 kérdés kész" badge, CTA „Kitöltöm"/„Folytatom" (nyíl nélkül) a
  /observe/[token] kitöltőre. Adat: team page receivedFeedbackRequests
  (inviter e csapat tagja, PENDING, nem lejárt, ObserverDraft-haladással)
  → ctx → OverviewTabView.
- Élőben: teszt-csapaton „Sárga Bence visszajelzést kér tőled · 12/60 ·
  Folytatom". tsc 0, 314 unit + 59 client, lint 0 error.

## Horgony-görgetés a linkeknél (site-szintű audit)

- ProfileTabs: generikus hash-görgetés mountkor — az App Router streaming
  miatt nem görget hash-re magától; ismételt ráigazítás (max 40×250ms),
  amíg a cél-elem a viewport tetejére nem kerül (layout-shift + háttér-tab
  eseteket is lefedi). Horgonyok: `#observer-flow` (gyűjtés-kártya),
  `#invitations` (meghívó-kezelő, minden ágban, scroll-mt-24-gyel).
- Link-audit eredménye — horgonyra állítva:
  - „Meghívók kezelése" CTA-k (csapat-banner, tag-nézet) → `#observer-flow`
  - OBSERVER_COMPLETED notif (meghívó) → `?tab=comparison#observer-flow`
  - MEASUREMENT_STEP_OPENED (observer, kész selffel) → `#observer-flow`
  - Meghívó-célú linkek (`?tab=invites` örökség): ComparisonTab,
    ObserverComparison, journey state/next-best-action,
    OBSERVER_INVITE_APPROVED/DECLINED notif, help-topics →
    `?tab=comparison#invitations`
  - Változatlan (tetőre mutatás helyes): RESULT_READY, OBSERVER_SUBMITTED,
    PURCHASE_CONFIRMED, holland-kod → tab=career, tab-paramos org/team linkek.
- Élőben: `#observer-flow` betöltés után a kártya a viewport tetején
  (scrollY 985, cardTop 96); `#invitations` a meghívó-blokkra visz.
  tsc 0, 314 unit + 59 client, lint 0 error/41 warning (baseline).

## Footer-hullám regresszió-fix: kattintás-lopás + takarás

A tegnapi varrat-fix (cf3dd99) -mt-10/14-gyel a megelőző oldal fölé húzza
a hullám-SVG-t — két mellékhatással: (1) az SVG átlátszó zónája elfogta
az alatta lévő gombok kattintását, (2) a rövid alsó paddingű oldalakon a
lap-alji elemek a hullám alá kerülhettek.

- Footer SVG: `pointer-events-none` — az átfedési zóna nem fog kattintást.
- Alsó padding-sweep (kontraktus: oldal-vég padding ≥ 56px md átfedés):
  pb-20-ra emelve — kitöltő-kliensek (team-role-360, trust, peer-feedback,
  psych-safety), /assessment/measurements, kampány-részletező + új-mérés
  oldalak (2-2 main), org/new, org-settings, observe-kitöltő, apply-kliens.
- Megjegyzés: az /assessment* és /try oldalakon a Footer eleve nem renderel
  (fókusz-mód) — ott a bug nem jelentkezhet.
- Mérve: results-oldalon SVG pointer-events none, utolsó interaktív elem
  89px-szel a footer teteje fölött. tsc 0, 314 unit + 59 client, lint
  0 error/41 warning.

## Csapatminta-buborék: üres tartalom fix

A csapat-oldali „Csapatminta" kártya „Elérhető" chipet mutatott, de a
törzse üres volt. Ok: a team-stats mintázat-bemenete a HEXACO
megjelenítési betűket (s.H/s.E/s.X/s.A/s.C/s.O) kereste a tag-score-okon,
miközben azok a nyers score-JSON BELSŐ kódjaival (INTE/RESO/TEMP/ADAP/
THOR/OPEN) jönnek — így soha nem gyűlt össze 3 „teljes" profil, a
patternResult mindig null lett (a TRITAN/HEXACO átállások öröksége,
48b75ea óta lappangott). Fix: a szűrés és a leképezés a belső kódokon.
Ellenőrizve: Horizont csapat (5 kitöltő) → patternResult 5/5 taggal,
fullLabel megjelenik. tsc 0, 314 unit + 59 client, lint 0 error.

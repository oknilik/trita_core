# 2026-07-28

## Tag-dossié (member dossier) — F1 KÉSZ

Read-only tag-összesítő az org admin + tanácsadó számára a
`/org/[id]/members/[userId]` route-on. Nem új mérés — a meglévő libek
személyre szűrt összeszerelése. Terv + vörös vonalak:
`docs/product/member-dossier-terv.md`. Lépésenként, 6 commit:

- `feat(dossier): canViewMemberDossier guard + invariant tests` —
  explicit allowlist (org admin + tanácsadói kör; ORG_MANAGER kizárva, a
  tag a sajátját sem éri el) a `measurement-auth.ts`-ben +
  `tests/unit/policy/member-dossier.test.ts`.
- `feat(dossier): pure member-dossier lib + team-stats friction exports` —
  `src/lib/member-dossier.ts` (Prisma-mentes típusok + `computeObserverAverage`
  [min-2 observer-küszöb], `computeDimComparisons`, `topGapDims`);
  `calculatePairFriction`/`frictionToEdgeType`/`DynamicsEdgeType` exportálva
  a `team-stats.ts`-ből.
- `feat(dossier): server assembler buildMemberDossier` —
  `src/lib/member-dossier.server.ts`: self + observer-aggregátum + csapatszerep
  self/peer + trust-beágyazottság (mért + becsült élek) + nevesített feedback
  + anonim darabszám + aktív kampányok. Vörös vonalak betartva: irányított
  trust SOHA (self-sor kizárva), observer csak aggregátum (min. 2), anonim
  feedback csak darabszám, pszich. biztonság pulse nem szerepel. Kilépett
  (`leftAt`) / nem-tag → null (a hívó 404-ez).
- `feat(dossier): member dossier page + read-only view` — a page guard-sorrend
  (requireOrgContext → canViewMemberDossier → buildMemberDossier → 404) +
  `MemberDossierView` (4 szekció: fejléc+részvétel, önkép-vs-külső küszöb-
  szövegekkel, kapcsolati beágyazottság, kapott visszajelzések).
- `feat(dossier): dossier entry points on org + team member lists` — Dossié
  gomb az org (`OrgPageShell`/`OrgMembersTab`) és a team (`TeamMembersTab`)
  tag-listáin; a gomb csak az engedett körnek renderelődik, mert a page
  számolja ki a `canViewMemberDossier`-t (`dossierBaseHref: string | null`
  prop) — kliens sosem hívja a guardot.

Verifikáció: tsc 0, `pnpm test:unit` 278/278, `pnpm test:client` 59/59.
Belépett vizuál (admin renderel / manager redirect) NEM tesztelve élőben —
a személyek nincsenek seedelve a Clerk dev instance-ban; kézi QA a seed
helyreállítása után.

## UX-audit végrehajtás — mind a 28 tétel (esti kör)

A 3-körös UX-audit riport (artifact) teljes listája megvalósítva, 7 commit:

- `refactor(team)`: team/[id]/page.tsx (1564 sor) → vékony elosztó + `_tabs/`
  nézetek (#11); TeamTabContext, intelligence-data helper.
- `fix(ux) szótár+címkék` (#12,#13,#16,#21): user-facing „kampány/kör" →
  „mérés"; OBSERVER_360 → „Külső visszajelzés (360°)", PEER_FEEDBACK →
  „Elismerés-kör" (CAMPAIGN_STEP_LABELS a kanonikus forrás); TRITAN enum-chip
  → „Kitöltve"; Eltávolítás-gombok halvány text-linkek + org-törlés
  megerősítéssel; PENDING_SETUP badge csak valódi hiánynál; dossié
  szerep-chip lokalizálva; címek „| trita".
- `fix(ux) gyors` (#1–6): org hero szám-chipek törölve (aside az egyetlen
  forrás); team hero dupla állapot-chip ki; dupla csapatkép-CTA ki;
  /dashboard sötét skeleton → RedirectLoader (márka-spinner);
  CompletionIndicator (gyűrű csak <100%, kész = pipa); tag felső navból
  az „Eredményeim" ki (user-menü a kanonikus).
- `feat(ux) team-navigáció+számok` (#7,#8,#14,#15,#18,#19): TeamTabBar
  (org-szerű fül-sor, back-linkek ki, hero-akciók karcsúsítva, egységes
  „Csapatkép megnyitása"); mintázat-blokk összevonás; befagyasztott
  aggregátum-számok öndefinícióval; pacing-csempe metrika kimondva;
  űrlapok fejléc-gomb mögé (+ Új csapat / + Tag meghívása / + Tag
  hozzáadása); ÜTEM magyarázat + sikeres-visszajelzés.
- `feat(ux) struktúra` (#9,#10,#17,#20): SurfaceHero mobil-karcsúsítás;
  /manager csak 2+ csapatnál (1 csapat → közvetlen /team); tag-felvétel
  „Hozzáadás" szemantika kimondott mellékhatással; org cockpit Jelöltek fül
  → 3-soros összegző + állandó /hiring link.
- `feat(ux) UI-elemek` (#22–28): results 5 fül → 3 (Eredmények+Munkastílus;
  „Külső kép" = összevetés+meghívók; régi ?tab= linkek átirányítva);
  kampánykártya szegmentált sáv + lenyíló részletek; wizard checkbox-sorok +
  élő sorrend-előnézet; egy-célpontú nav-dropdownok → link; mobilmenü egy
  szint; függő meghívók összecsukott szekcióban; státusz-chipek nagy-szám
  tipográfia helyett.

Kapuk minden commitnál: tsc 0 · unit 278/278 · client 59/59; smoke a bypass
szerveren (org/team/wizard/hiring/results 200). Frissített tesztek: nav-
visibility (results nem topnav-elem), page-action-gating (fejléc-gombos
űrlapnyitás).

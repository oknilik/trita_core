# Changelog — 2026-04-03

## Belbin → Csapatszerep átnevezés (teljes kódbázis)

Zero-migration átnevezés: a DB tábla- és oszlopnevek maradnak (`@@map` / `@map`), de a kód szintű nevek mindenhol változnak.

**Prisma schema:**
- `BelbinAnswer` → `TeamRoleAnswer` (@@map: BelbinAnswer)
- `BelbinScore` → `TeamRoleScore` (@@map: BelbinScore)
- `belbinRoundActive` → `teamRoleRoundActive` (@map)
- `belbinRoundStartedAt` → `teamRoleRoundStartedAt` (@map)
- `belbinAnswerId` → `teamRoleAnswerId` (@map)

**Fájl átnevezések (13 fájl):**
| Előtte | Utána |
|--------|-------|
| `belbin-scoring.ts` | `team-role-scoring.ts` |
| `belbin-estimate.ts` | `team-role-estimate.ts` |
| `belbin-questions.ts` | `team-role-questions.ts` |
| `BelbinQuestionnaire.tsx` | `TeamRoleQuestionnaire.tsx` |
| `TeamBelbinSection.tsx` | `TeamRoleSection.tsx` |
| `BelbinRoundCard.tsx` | `TeamRoleRoundCard.tsx` |
| `BelbinTeaser.tsx` | `TeamRoleTeaser.tsx` |
| `BelbinRoles.tsx` | `TeamRoles.tsx` |
| `PdfBelbin.tsx` | `PdfTeamRoles.tsx` |
| `/assessment/belbin/` | `/assessment/team-roles/` |
| `BelbinClient.tsx` | `TeamRolesClient.tsx` |
| `/api/belbin/submit/` | `/api/team-roles/submit/` |
| `/api/team/belbin-round/` | `/api/team/role-round/` |

**Kód szintű cserék:**
- `BELBIN_ROLES` → `TEAM_ROLES`
- `BELBIN_SCORING_MAP` → `TEAM_ROLE_SCORING_MAP`
- `BELBIN_SECTIONS` → `TEAM_ROLE_SECTIONS`
- `HEXACO_BELBIN_WEIGHTS` → `HEXACO_TEAM_ROLE_WEIGHTS`
- `BelbinRoleCode` → `TeamRoleCode`
- `BelbinScores` → `TeamRoleScores`
- `BelbinAnswers` → `TeamRoleAnswers`
- `calculateBelbinScores()` → `calculateTeamRoleScores()`
- `estimateBelbinFromHexaco()` → `estimateTeamRolesFromHexaco()`
- `BELBIN_STARTED` / `BELBIN_COMPLETED` → `TEAM_ROLE_STARTED` / `TEAM_ROLE_COMPLETED`

**Dokumentáció:** minden docs/ MD fájlban Belbin referenciák cserélve.

**Ellenőrzés:** 0 maradék `belbin`/`Belbin` referencia a `src/` és `docs/` könyvtárakban. 43/43 journey teszt zöld.

---

## Stripe / Billingo integráció — P0 implementáció

### A1 — Billing domain audit (`docs/audits/billing-domain-audit.md`)
- Meglévő Subscription/Purchase/CandidateCredit mezők dokumentálva
- Hiányzó mezők és új modellek azonosítva

### K2 — Event matrix (`docs/architecture/billing-event-matrix.md`)
- 7 Stripe event → Trita action → Billingo action → local state effect mátrix
- Trigger szabályok és idempotency kulcsok dokumentálva

### A2 — Prisma schema bővítés
- Subscription: +`stripeLatestInvoiceId`, `planType`, `billingInterval`, `currentPeriodStart`, `billingoLastDocumentId`
- Purchase: +`productType`, `grossAmount`, `billingoDocumentId`, `billingoDocumentNumber`, `invoiceStatus`
- Új modellek: `BillingEventLog`, `BillingoPartnerLink`, `BillingDocumentLink`

### B1 — Stripe metadata schema (`src/lib/billing/stripe-metadata.ts`)
- `buildCheckoutMetadata()` builder + `parseCheckoutMetadata()` parser
- Typed `StripeSessionMetadata` interface
- 8 unit teszt

### B2 — Checkout metadata refactor
- `checkout/route.ts` és `purchase/route.ts` — `buildCheckoutMetadata()` hozzáadva a meglévő ad-hoc metadata mellé
- Backward compatible (régi kulcsok megtartva)

### H1 — Country/VAT decision hook (`src/lib/billing/vat-decision.ts`)
- `resolveVatDecision()` — HU domestic / EU B2B reverse charge / international
- 10 unit teszt (HU, DE+VAT, FR+VAT, DE no VAT, US, edge cases)

### C1 — Billingo API kliens (`src/lib/billing/billingo-client.ts`)
- Fetch-based Billingo v3 kliens
- Partner CRUD, invoice create, correction, rate limit retry

### C2 — Partner resolver (`src/lib/billing/partner-resolver.ts`)
- `resolvePartner()` — user/org → Billingo partner link
- `resolvePartnerForPurchase()` — product type alapján user vs org

### D1 — Webhook handler refactor
- `route.ts` 321 sor → 85 sor dispatch layer
- 5 handler fájl a `src/lib/billing/handlers/` alatt:
  - `shared.ts`, `checkout-completed.ts`, `subscription-sync.ts`, `invoice-paid.ts`, `invoice-failed.ts`

### D2 — Idempotency layer (`src/lib/billing/idempotency.ts`)
- Event-id alapú deduplikáció a `BillingEventLog` modellel
- `isEventProcessed()`, `markEventProcessing()`, `markEventProcessed()`, `markEventFailed()`, `markEventSkipped()`

### E2+F3 — Invoice item mapping (`src/lib/billing/invoice-items.ts`)
- Purchase: self_plus, team_snapshot, team_deep_dive, candidate_pack (1/5/10)
- Subscription: team/org × monthly/annual
- EUR/HUF konverzió + VAT code mapping
- 8 unit teszt

### P1 batch

**C3 — Invoice payload normalizer** (`src/lib/billing/invoice-normalizer.ts`)
- `normalizeInvoicePayload()` — VAT decision + items → Billingo-ready payload
- `buildPurchaseInvoicePayload()` / `buildSubscriptionInvoicePayload()` — convenience wrappers

**I2 — Retry mechanizmus** (`src/lib/billing/retry.ts`)
- `listRetryableEvents()` — admin listing
- `resetEventForRetry()` — manuális újrafuttatás előkészítése
- `getRetryStats()` — admin dashboard stats

**J2 — Webhook handler contract tesztek** (`tests/unit/billing/webhook-handlers.test.ts`)
- 10 teszt a teljes billing láncra: metadata → parse → VAT → invoice item → payload
- HU/EU B2B/US forgatókönyvek
- Dátum, paymentMethod, multi-item edge case-ek

**K1 — Source of truth doc** (`docs/architecture/billing-source-of-truth.md`)
- Stripe = payment, Billingo = compliance, Trita = orchestration
- Adat flow diagram és "mi hol él" táblázat

**K3 — Product billing mapping** (`docs/architecture/billing-product-mapping.md`)
- Minden termék → Stripe price → Billingo tételnév → trigger event
- Metadata contract és hiányzó .env price ID-k

### P2 batch

**G1 — Refund handler** (`src/lib/billing/handlers/refund.ts`)
- `charge.refunded` event kezelés
- Eredeti Billingo dokumentum lookup → correction document create
- Purchase status és invoiceStatus frissítés
- Teljes vs részleges refund megkülönböztetés
- Bekötve a webhook dispatch-be

**G2 — Correction policy** (`docs/architecture/billing-correction-policy.md`)
- Teljes refund → automatikus stornó
- Részleges refund → manuális (V2-ben automatikus)
- Dispute → manuális

**I1 — Billing reconciliation admin endpoint** (`src/app/api/admin/billing/route.ts`)
- `GET /api/admin/billing` — event log (100 legutóbbi) + retry stats + retryable events
- `POST /api/admin/billing` — failed event reset for retry
- Admin-only (`requireAdmin()`)

**I3 — Structured tracing** (`src/lib/billing/tracing.ts`)
- `traceBillingEvent()` — JSON formatted billing log
- `withBillingTrace()` — handler wrapper (auto start/success/failure logging)
- Mezők: stripeEventId, eventType, productType, billingoDocumentId, resultStatus, durationMs

**F5 — Subscription updated handler bővítés**
- Plan/quantity change logolás
- Cancel flag változás logolás

**F6 — Subscription deleted handler bővítés**
- Explicit `status = "canceled"` update a local DB-ben

### v2 diagram → kód átvezetés

**invoiceStatus state machine pontosítás**
- Refund handler: `voided` → `corrected` (purchase + BillingDocumentLink)
- Purchase flow: `pending` → `issued` / `failed` (intermediate state)

**Tracing bekötés minden handler-be**
- checkout-completed: `withBillingTrace()` wrapper
- invoice-paid, invoice-failed, subscription-sync, refund: `traceBillingEvent()` hívások

**Billingo invoice chain implementáció (nem marad TODO)**
- checkout-completed: one-time purchase + candidate addon → teljes Billingo chain (partner → VAT → item → normalize → create)
- invoice-paid: recurring subscription → teljes Billingo chain (idempotent existing doc check-kel)
- Billingo hiba: non-blocking — purchase/subscription sync már megtörtént, invoice retry lehetséges

**Order confirmation email áthelyezés**
- Eltávolítva: `subscription-sync.ts` (trialing→active transition)
- Hozzáadva: `invoice-paid.ts` (első fizetett invoice = canonical payment trigger)

**Stripe SDK type kompatibilitás**
- `Invoice.subscription` és `Charge.invoice` property-k cast-olva a strict Stripe v2025 típusokhoz

### Review feedback → policy dokumentumok

**Billing Failure Policy** (`docs/architecture/billing-failure-policy.md`)
- Non-blocking Billingo szabályzat: "Stripe sync first, invoice second"
- Retry source of truth, duplikáció védelem, max 3 retry
- Anti-pattern lista (ne legyen blocking, ne retry-olj szinkron, ne a return page-ről)

**Billing Access States** (`docs/architecture/billing-access-states.md`)
- Provisioning vs entitlement vs payment szétválasztás
- State machine: none → trialing → active → past_due/frozen/canceled
- Policy mapping: Stripe status → policyState → capabilities
- Org activation timing: ACTIVE org status ≠ fizető ügyfél

**Current vs Target összefoglaló** (`billing-architecture-diagrams.md` kiegészítés)
- 10 implemented terület
- 4 target (V2) terület
- 1 conceptual

### Tesztek összesítés
- 36 billing unit teszt (metadata, VAT, invoice items, webhook chain) — mind zöld
- 43 journey teszt — mind zöld
- Pre-existing hibák nem változtak

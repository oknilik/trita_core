# 2026-07-23

## Jelölt-flow újraélesztés (délelőtti commitok)

- `cfcdc1d feat(candidate)`: a fagyasztott hiring/candidate réteg vissza az
  aktív fejlesztésbe — kapu ki (`CANDIDATE_GATING_ENABLED=false`,
  operating-mode, visszakapcsolható); hozzáférés = isConsultantSurface
  (consultant / platform-tanácsadó / trita-admin) minden felületen + API-n.
  `CandidateInvite.orgId` (backfillelve) — team-nélküli jelölt is kezelhető;
  `includeTeamRole` flag + `CandidateResult.teamRoleSelections`; apply-flow:
  TRITAN → opcionális csapatszerep-kérdőív 2. lépés (átugorható). Org cockpit
  „Jelöltek" fül + teljes felület `/hiring/[orgId]`. Bugfix: candidate-submit
  completeness a `DEFAULT_ASSESSMENT_FORM`-hoz mér (előtte full bankot várt,
  minden beadás bukott).
- `9a34370 feat(candidate)`: kitöltés-értesítés — in-app notif + HU/EN email
  a tanácsadónak és org adminnak, E2E verifikálva.

## PDF borítóoldal javítás

- **Bug**: a letöltött riport borítója két oldalra esett szét — 1. oldal csak a
  gradiens háttér, 2. oldal fehér lapon láthatatlan (fehér-fehér) szöveg, csak
  a narancs archetípus látszott.
- **Ok**: a `CoverPage`-ben a háttér-`Svg` közvetlenül kapott
  `position: absolute` style-t, amit a react-pdf `Svg` elemen nem vesz fel —
  a 842pt magas Svg flow-ban maradt, és minden utána jövő elemet átlökött egy
  túlcsordulás-oldalra.
- **Fix**: az Svg abszolút pozicionált `View` wrapperbe került
  (`src/components/pdf/pages/CoverPage.tsx`). Megjegyzés: `wrap={false}` a
  Page-en NEM használható — react-pdf bug (`unsupported number: Infinity` a
  renderSvg/resolveAspectRatio útvonalon abszolút wrapper + Svg mellett).
- Verifikáció: node-oldali `renderToBuffer` próbarender mock adatokkal, a
  borító egy oldalon, helyes tartalommal renderel.

## PDF tartalom-finomítás

- „Következő lépés" blokk (observer meghívó CTA) kikerült a PDF-ből:
  `PdfNextStep` komponens törölve, `PlusWorkStylePage` hívás + i18n kulcsok
  (`pdf.nextStep`, `pdf.nextStepObserverPage`, `pdf.nextStepSendInvite`)
  eltávolítva.
- `BLOCK8` záró mondat átírva (`src/lib/profile-content.ts`, HU+EN): „A többi
  már rajtad múlik" kikerült, helyette visszatérésre hívó, támogató zárás.
  Egyetlen forrás — results oldal, dashboard ProfileInsights és PDF closing
  is innen kapja.

## Csapat-hero élő pillanatkép = org-hero design

- A team-oldal hero „Élő pillanatkép" aside-ja átállt az org cockpit hero
  nyelvére: a két lapos progress-sáv helyett ProgressRing-gyűrűk
  (76px, `bg-white/[0.06]` csempék, felirat + kész/folyamatban sor alatta),
  a számláló-csempék `tabular-nums`-szal. Szín-logika változatlan
  (#8ad0b4 kitöltés + teamHeroTheme.primary másodlagos).

## Blog: 2 új cikk a napi riport trend-ötleteiből (HU+EN párban)

- „Mérhető-e a pszichológiai biztonság? Így mérjük mi" (`merheto-e-a-
  pszichologiai-biztonsag` + `can-psychological-safety-be-measured`) —
  a riport 5.4 ötlete: Edmondson/Project Aristotle/COE 47 ország/Gallup 34%
  keretezés, a PSYCH_SAFETY pulse-modul elveivel (8 item, anonim, n≥3).
- „Toborzás helyett: a belső mobilitás a megtartás csendes fegyvere"
  (`belso-mobilitas-a-megtartas-csendes-fegyvere` +
  `internal-mobility-quiet-retention-weapon`) — a riport 5.3 ötlete: SHRM
  2026 (93% rotáció-hatékonyság, <25% adopció) + csapatszerep-mátrix érv.
- Mindkettő böngészőben verifikálva (lista + cikkoldal, StatRow/Callout/
  KeyInsight render, konzol tiszta).

## Radar-legend csere: SVG-belső → HTML jegyzet az ábra alatt

- A RadarChart SVG-belső legendje (fix 204px rect + fix szöveg-koordináták a
  viewBox alján) hosszabb feliratnál — jelölt-név, csapatnév — elcsúszott.
- Legend törölve az SVG-ből (`selfLabel`/`observerLabel` prop is megszűnt);
  új komponens: `src/components/dashboard/RadarLegendNote.tsx` — kis HTML
  szín-jegyzet az ábra alá (pont + felirat, természetes tördelés; default
  feliratok: comparison.self/others).
- Bekötve mind a 4 összehasonlító helyen: ObserverComparison,
  DimensionOverview (csak showObserver-nél), hiring jelölt-részletező
  (jelölt vs. csapat), TeamReportMemberView (Te vs. Csapatátlag).

## SuccessCheck primitív

- Új: `src/components/ui/primitives/SuccessCheck.tsx` — egységes siker-pipa
  (sage kör, fehér ✓, a share modal nyelve).
- PDF-letöltés gomb pipája (`ProfileHero`, korábban emerald) és a ShareModal
  két inline pipája (link másolva, email elküldve) erre cserélve.

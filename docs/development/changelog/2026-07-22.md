# 2026-07-22

> Utólag pótolt bejegyzés (2026-07-23) — a napi riport jelezte a kimaradást.
> Forrás: git log (26 commit) + `docs/security/security-audit-2026-07-22.md`
> + `docs/development/ui-unification-plan.md`.

## Biztonsági sprint (P1–P8)

Teljes security audit (`docs/security/security-audit-2026-07-22.md`), a
tételek nagy része aznap javítva:

- **P1a**: named org-/csapat-meghívó csak a címzett fiókjával fogadható el.
- **P1b**: team/org join-link kripto-random tokenre cserélve (nem a cuid id).
- Token-entrópia növelés, email-endpointok rate limitje, biztonsági headerek.
- Cron-endpoint timing-safe összehasonlítás; `features/interest` rate limit;
  remind 401 JSON-válasz.
- **P2**: CSP Report-Only élesítés. **P8**: Resend from-domain
  prod-figyelmeztetés (az ops-teendő — domain-verify + `RESEND_FROM_EMAIL` —
  még nyitott).
- **P3**: org role-PATCH authz-invariáns tesztek; **P5**: feature-interest
  közös vokabulárium (a végpont-egységesítés külön refaktor-körre maradt).

## UI-egységesítés (F1–F3 + follow-up)

Terv: `docs/development/ui-unification-plan.md` — mind a négy kör lezárva:

- **F1 alapozás**: típus-skála tokenek (text-display…micro), eyebrow-
  variánsok, emoji→SVG, 10px lint-padló.
- **F2 konszolidáció**: közös token-modul (`design-tokens.ts` = TS-oldali
  szín-forrás, globals.css-szinkront teszt őrzi), primitív-adopció.
- **F3 élmény-réteg**: reveal, slotok, archetípus-kártya, ProgressRing,
  CountUp, CelebrationBurst (reduced-motion-tudatos).
- **Follow-up**: PDF-borító (archetípus-cover), email hero-nyelv,
  ikon-készlet, tabular-nums sweep.

## Share-flow és PDF

- Megosztás-kezelő modal (link létrehozás/másolás, email-küldés, visszavonás);
  munkastílus-szekció a `/share` nézetben (`workstyle-content.ts` közös lib);
  visszavont link → barátságos oldal.
- PDF: levegősebb kártya-layout, TRITAN-sorrendű radar chart, letöltés-gomb
  visszajelzés (spinner + pipa).
- Fordítás-parsing javítás, EN-fallback jelzések, melegebb PDF-dizájn.

## Admin és inquiry-pipeline

- Inquiry modell: `/contact` + in-app help-widget → `lib/inquiries.ts`;
  admin „Kérdések" tab; org cockpit tanácsadói Kérdések fül;
  `INQUIRY_RECEIVED` értesítés szerep szerinti linkkel.
- Admin: Kutatás tab törölve, `page.tsx` → `_tabs/` szétszedés,
  user-menüben Admin vezérlő link (isPlatformAdmin).

## Egyéb

- `chore(prisma)`: külön migrációs fájl visszavonva — a projekt db push
  workflow-t használ.

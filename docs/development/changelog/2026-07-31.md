# 2026-07-31

Hat commit, egy szálon: a karrier-iránytű **szakmacsalád-rétege** — előbb a
váz dokumentumban (19 → 20 → 21 család), aztán a katalógusban, végül a
motorban. A nap végén a rangsor **súly-sorrendje** is megfordult, és közben
előkerült egy valódi adatvesztéses bug.

## Szakmacsalád-váz — 19 → 20 → 21 család

Három dokumentum-commit, mielőtt bármi kódba került volna. A levezetés és a
mérések: `docs/product/career-families.md`.

`04f9be5` — **a váz véglegesítve, 19 család.** A hozzárendelés **hibrid**:
szemantikus korlát (ISCO-alcsoport halmaz családonként) + viselkedés-alapú
döntés a korláton belül (6 dimenzió-szint + 7 tengely + RIASEC dupla súllyal,
a horgonyok centroidjához mérve).

- *Miért kell a szemantikus korlát:* tisztán viselkedés-alapon a Pék a
  logisztikába, az Autóbuszvezető a vendéglátásba került — viselkedésileg
  közeliek, a felhasználónak képtelenség.
- *Miért nem futásidőben számolunk:* sima k-means 8 maggal futtatva a párok
  **13,6%-a ingadozik**, és a foglalkozások **91%-a vándorol**. A besorolás
  ezért egyszer készül és BEFAGY.

Mért eredmény: 19 család, 477 foglalkozás, 0 besorolatlan, 71 tétel kézi
review-ra. Három család már szállítható (oktatás 0,55 · építés 0,70 · gyártás
0,70), négy szórása kiugró (pénzügy 1,19 — az ISCO 24 túl tág; ápolás 1,28;
segítő 1,25; személyes 1,37). Mérlegelt kompromisszum: 19 család az eredetileg
javasolt 12–16 helyett.

`9ebe7a3` — **külön jog és hatósági család, 20 család.** Új `jog` család (13
tétel: Ügyvéd, Törvényhozó, Adóügyintéző, Regulatory affairs, Vámügyintéző,
Bírósági ügyintéző, Jogi titkár, Adóellenőr, Engedélyezési ügyintéző, Műszaki
vizsgabiztos, Teherárú-felügyelő, Jogi asszisztens). Ehhez a mechanizmus is
finomodott: a lapos ISCO-halmaz helyett **leghosszabb illeszkedő prefix nyer**,
plusz tételes besorolás (pl. az Adóügyintéző az ISCO 2411-en ül a könyvelőkkel,
mégis a joghoz tartozik). Enélkül a jogi asszisztens a 34-es segítő családba
esne.

A kiemelés négy másik család szórását is javította, mert a jogi/hatósági
tételek addig szét voltak szórva köztük: pénzügy 1,19 → 1,05 · ápolás 1,28 →
1,05 · segítő 1,25 → 1,06 · személyes 1,37 → 1,09.

> **Nyitott döntés:** a bíró, közigazgatási bíró, mediátor, bírósági fogalmazó
> és jegyzőkönyvvezető T3-ként (niche) az archívumban van, nem a termékben. Ez
> a user saját Excel-validálásának eredménye, ezért nem lett felülírva —
> döntést igényel, hogy átemeljük-e őket T2-be (477 → 482).

`c9d94a1` — **HR a vezetéshez, szépségipar külön család, 21 család.**
User-döntések: a HR-szakmák (ISCO 2423, 2424) a pénzügyből a vezetés családba
kerülnek; a szépségipar önálló család lesz; az ápolás külön marad.

- vezetés 36 → 42 tétel (0,87 → 0,91)
- pénzügy 55 → 49 tétel (1,05 → 1,04)
- szépségipar: új, 4 tétel, szórás **0,60** — a legszorosabb család az egész
  készletben (Fodrász, Férfifodrász, Kozmetikus, Manikűrös)

A `szemelyes` maradéka `szolgaltatas` néven él tovább (14 tétel, 1,15) — ez
gyűjtőkosárra sikeredett (Fitneszedző, takarítók, Futár, Légiutas-kísérő,
Idegenvezető, Jegyszedő). Jelölve, de a „többi maradjon most" alapján nem lett
tovább bontva; **ez a következő gyenge pont.**

## Család-réteg a katalógusban — befagyasztott besorolás

`448500b` — a 477 foglalkozás 21 családba sorolva, kódban.

Új/változott:

| Fájl | Szerep |
|---|---|
| `src/lib/career/catalog/families.json` | a 21 család konfigurációja |
| `scripts/career-catalog/step12_families.mjs` | a hozzárendelő (Node, mert nem külső forrást dolgoz fel, és ugyanazt a configot olvassa, mint a TS) |
| `src/lib/career/families.ts` | metaadat, kétnyelvű címke + definíció |
| `Occupation.family` / `OccupationFit.family` | a besorolás átvezetése |
| `tests/unit/career/families.test.ts` | 7 teszt |

A tesztek **két valódi hibát kaptak el már az első futásnál**:

1. A Szakápoló a `gyogyitas`-ba esett, vele 8 diplomás ápoló és szülésznő
   (ISCO 222x) — közben az Ápoló (3221) az `apolas`-ban volt. Javítva: a 222
   az `apolas` prefixei közé került.
2. Az Ingatlanügynök a `penzugy`-be esett, vele 10 értékesítő szerep — közben
   az `ertekesites` mindössze 8 tételes volt. Javítva: a 332 mindkét családhoz
   (ott a viselkedés dönt), a 3334/3339 az `ertekesites`-hez.

Hatás: értékesítés 8 → 15 (1,04 → 0,96), gyógyítás 33 → 25, ápolás → 40.

343 unit teszt zöld, type-check 0 hiba, lint tiszta.

## Családszintű illeszkedés a motorban + diverzifikálás valódi családra

`27386ff` — két bekötés a tényleges számításba.

1. **`aggregateFamilyFits`** (`family-fit.ts`) — családszintű illeszkedés a
   TELJES pontozott halmazból, a limit és a diverzifikálás **előtt** (különben
   a család pontszáma attól függne, hány tétele fért be a listába). A család
   pontszáma a legjobban illeszkedő tagok átlaga: nem az összesből (egy nagy
   család akkor is jó irány lehet, ha a tagjainak fele nem illik), és nem a
   legjobb EGYből (az egy kiugró tétel zajára ülne rá). Top-K =
   `clamp(ceil(n/3), 1, 8)`. A hibasáv **nem szűkül** √k-val: a tag-pontszámok
   erősen korreláltak (ugyanaz a személy, hasonló szerepek), a független minták
   feltevése hamis pontosságot mutatna — a tagok hibájának átlagát vesszük.
2. **`diversify`** mostantól a VALÓDI családra fut, nem az ISCO 2-jegyű
   alcsoportra. A függvény saját kommentje eddig is „szakmacsaládot" emlegetett
   — az ISCO csak közelítette, és képzettségi szint szerint vágott, nem
   munkatartalom szerint.

`CareerFitResult.families` és `CareerResultView.families` — **a szerződés kész,
az API már szállítja, a UI még NEM használja.**

349 unit teszt zöld, type-check 0 hiba, lint tiszta.

### NYITOTT — a család-rangsor megkülönböztethetősége

Három valós profilon lefuttatva a családpontszámok 85–91 közé tömörülnek, a
hibasávok viszont ±10..16 — egyik profilnál sem különböztethető meg az 1. és a
2. család. A „legjobb K tag átlaga" minden családnál magas értéket ad, mert
bármelyikben akad néhány jól illeszkedő szakma. A `familiesAreDistinguishable`
ezt helyesen jelzi, de a metrika így még nem alkalmas rangsorolásra.
**Javaslat a commitban:** a család SAJÁT középpontjához mérni (mennyire illik a
család TIPIKUS profiljához), nem a legjobb tagjaihoz.

**Utólagos mérés (2026-07-31, ugyanaznap):** a diagnózis részben pontosításra
szorul. Három szintetikus profilon a család-fit tartomány valójában 74–95 /
61–86 / 46–73 (szórás 5,8 / 7,3 / 8,5) — a tömörülés **profilfüggő**, lapos
profilnál erős, markánsnál nem. A tényleges szűk keresztmetszet az átlagosan
**11,6-os `rankSe`**, ami a személy dimenzió-mérési hibájából propagálódik, nem
az aggregálásból. Bootstrap (400 iteráció, a dimenziókat a mérési hibán belül
perturbálva) — mennyire reprodukálható a rangsor:

| Metrika | 1. hely stabil (A / B / C profil) |
|---|---|
| top-K (jelenlegi) | 15% / 59% / 53% |
| középpont (összes tag átlaga) | 29% / 81% / 61% |

Két következtetés: (a) a `familiesAreDistinguishable` **nem túl konzervatív,
igazat mond** — a megkülönböztethetetlenség valós mérési tulajdonság; (b) a
javasolt középpont-irány **empirikusan javít**, de önmagában nem elég, mert a
legjobb variánsnál is minden 5. felhasználó más első családot kapna
újramérésnél. Mellé kell egy megjelenítési szabály is: rangsort csak a
stabilitási kritérium teljesülésekor, egyébként sáv/csoport. Megjegyzendő az is,
hogy a középpont-metrika nem sorrend-őrző csere (a C profilnál más családot hoz
elsőnek), tehát a mögöttes tartalmakat is újra kell kalibrálni.

*A javítás nincs beütemezve — a család-réteg UI-bekötése addig nem indul.*

## A kimondott preferencia lett a rangsor legerősebb jele

`c6f4bde` — sorrend mostantól: **kimondott szándék → személyiség → érdeklődés.**

```
RANK_WEIGHTS    demand 0,55 / interest 0,30 / preference 0,15
             →  preference 0,45 / demand 0,35 / interest 0,20
CHOICE_WEIGHTS  interest 0,70 / preference 0,30
             →  preference 0,65 / interest 0,35
```

**Indok:** a preferencia az EGYETLEN komponens, amit a felhasználó kimondott. A
személyiség mért becslés, a Holland-kód sokszor maga is becsült. Ha valaki azt
mondja, nem akar emberekkel dolgozni, azt nem írhatja felül egy 60 itemes
műszerből származó profil-hasonlóság. A súlyok a MEGLÉVŐ komponensekre
normálódnak újra, tehát aki egy tengelyt sem állított be, nem jár rosszabbul.

**Közben talált hiba — ez volt az igazi ok, amiért a preferenciák nem értek
semmit:** a `prefs` és a `leadIntent` ÜRESEN inicializálódott, nem a mentett
háttérből. És mivel mentésnél `{ ...background, prefs, leadIntent }` megy ki,
az üres `prefs` **felülírta** a szerveren tárolt beállítást — a „válaszok
módosítása" tehát törölte a felhasználó saját preferenciáit. Javítva: mindhárom
állapot (`prefs`, `leadIntent`, `leadChoice`) a mentett háttérből indul.

**Holland-eredmény:** csak ÚJRAKEZDÉSKOR (DELETE) veszik el, módosításkor soha.
A POST teljes cserét ír, ezért a szerveren őrizzük meg: ha a beküldött háttér
nem hozza a `riasecScores`-t, a meglévőt megtartjuk. A kitöltés a leghosszabb
lépés — egy hiányos kliens-payload nem törölheti el.

**Mért hatás:** „nem emberekkel + strukturált" preferenciára a top-10 MIND a 10
tétele kicserélődött.

> Megjegyzés: erős preferenciáknál a korlátokat tökéletesen teljesítő,
> alacsonyabb képzettségű szerepek is felkerülhetnek a jobb személyiség-
> illeszkedés elé — ez a kért viselkedés egyenes következménye, a 0,45-ös súly
> hangolható.

351 unit teszt zöld (2 új a súly-sorrendre), type-check 0 hiba, lint tiszta.

## Karrier-háttér a pilot-forgatókönyvbe — S9 (csak dokumentáció)

Az F3 known-groups validáció **n=0**-n áll, és nem véletlenül: a
`CurrentRolePicker` az eredmény-oldal karrier-blokkjának **alján, passzívan**
ül — nincs mögötte kampány-lépés, értesítés vagy emlékeztető, tehát aki
megnézi az eredményét, jellemzően nem görget odáig. Ez viszont a validáció
egyetlen adatforrása: ha a pilot alatt sem gyűlik, a szakdolgozat validációs
fejezete a pilot UTÁN is üres marad.

Amíg a mező nem kap saját kampány-lépést, a tanácsadói kérés pótolja:

- `pilot-playbook.md` — új **S9** sikerkritérium (≥60% a kitöltőkből megadja a
  jelenlegi munkáját, forrás: `careerBackground.currentOccupationId`), plusz
  konkrét teendő a 2–3. hét (önértékelés + observer-kör) szakaszban, javasolt
  megfogalmazással.
- `kampany-utemterv.md` — az 1. lépés (OBSERVER_360) tanácsadói sorába
  bekerült, indoklással.
- `career-engine-plan.md` — az F3 szakasz mostantól kiírja, **hogyan** gyűlik
  össze az adat, és mi a következő lépés, ha a pilot alatt sem elég (a mező
  saját kampány-lépéssé emelése).

Haladás-ellenőrzés: `npx tsx scripts/career-validation/report.ts` → „megadott
jelenlegi foglalkozás: N fő". Admin felületen nincs rá nézet.

## Interakció-szimuláció F2 — kompozíciós motor KÉSZ

`src/lib/interaction-engine.ts` — a terv szerint aug 3–4-re ütemezve, előrehozva.
Tiszta függvény: `simulateInteraction({ self, other, mode, level })` → három
blokk (*ami magától megy* · *ahol súrlódás várható* · *mit beszéljetek meg
előre*), az F1 atom-készletből válogatva.

**A csővezeték:**

1. **Polarizálás** — a `profile-engine.ts` konvenciójával (HIGH 65 / LOW 35,
   szigorú összehasonlítás). A középsáv szándékosan néma: kiegyensúlyozott
   dimenzióról nem állítunk dinamikát.
2. **Jelöltek** — a két fél pólusos dimenzióinak minden párosítására megy egy
   `findAtom` hívás; ez egy menetben lefedi az azonos dimenziós ÉS a
   kereszt-párokat is, saját index nélkül.
3. **Markánsság** = `FRICTION_WEIGHTS` (keresztnél a két dimenzió átlaga) × a
   pólusok erőssége. Az erősség a **gyengébb** oldalról jön: egy alig pólusos
   fél nem tesz erőssé egy dinamikát.
4. **Válogatás** — a legerősebb 2–3 atom, de csak az kerül be, ami ÚJ dimenziót
   hoz (különben három egymás alatti THOR-szöveg jönne). Azonos markánsságnál
   az atom-ID dönt, hogy a kimenet determinisztikus legyen.
5. **Vezető-mód** — a `LEADER_SUPPLEMENTS` a vezető pólusos dimenzióira, súly
   szerint, külön blokként.

**Döntések, amiket érdemes rögzíteni:**

- **A motor locale-független**: kétnyelvű `LocalizedText`-et ad vissza, a
  nyelvet a megjelenítő réteg választja. Így az F3-ban szerver oldalon egyszer
  lefuttatható mind a 30 archetípusra, és csak a kért nyelv szerializálódik —
  se atom-tartalom a kliens bundle-ben, se roundtrip archetípus-váltáskor.
- **Adatvédelem NEM a motorban**: pontszámokat kap, hozzájárulásról nem tudhat.
  A „konkrét kolléga csak megosztott profillal vagy közös csapat-tagsággal"
  korlát a hívó (page/route guard) dolga — a modul kommentje ezt kimondja.
- **`sparse` jelzés**: ha egy atom sem aktiválódik, az nem üres lista, hanem
  explicit jelzés — a felület külön üzenettel kezeli („nincs elég markáns
  eltérés köztetek"), nem találgat.
- **Archetípus-prototípusok**: a terv „86/74-es receptje" eddig nem létezett a
  kódban. Most **származtatott** (domináns 86 / második 74 / maradék 50), nem
  kézzel írt tábla. Következmény, amit az F3-nak ki KELL írnia: a maradék négy
  dimenzió a középsávban marad, tehát archetípus-választásnál csak a domináns
  és a második dimenzió aktivál atomot. Ez helyes — egy típuscímke tényleg nem
  mond semmit a másik négyről —, de enélkül a „típus-szintű becslés" jelölés
  üres udvariasság lenne.

**Mellékesen feloldott duplikáció:** a `FRICTION_WEIGHTS` két helyen élt
(`team-stats.ts` + `DynamicsMap.tsx` saját másolata), mert a `team-stats`
behúzza a Prismát, így kliensről nem importálható. Harmadik másolat helyett a
modell átkerült a tiszta `src/lib/friction-model.ts`-be; a `team-stats`
változatlanul re-exportálja, tehát a meglévő importok érintetlenek, a
`DynamicsMap` pedig a közös forrásra áll.

**Verifikáció:** 20 új unit teszt (`tests/unit/platform/interaction-engine.test.ts`),
összesen **371 zöld** (351 → 371), type-check 0 hiba, lint 0 error — az új
fájlokban warning sincs. A tesztek lefedik: a küszöb-konvenciót, a
determinizmust, a tükrözést (aszimmetrikus atomnál a két oldal tényleg más
szöveget kap), a `FRICTION_WEIGHTS` szerinti sorrendet, a dimenzió-dedupot, a
vezető-mód irányát, a `sparse` esetet, és hogy **mind a 30 atom elérhető**.

## Interakció-szimuláció F3 — web UI KÉSZ

A results-oldal új szekciója („Hogyan működnétek együtt?"), a terv szerint aug
5–7-re ütemezve, előrehozva. Az F1-ben rögzített döntés szerint **szekció, nem
negyedik fül** — a `ProfileTabs` navigációjához nem kellett nyúlni; a
csapatszerepek UTÁN ül, mert az egyéni működésmódról lép tovább a kétszemélyes
dinamikára.

- **`src/lib/interaction-view.ts`** — nézet-modell építő. A szerveren lefut
  mind a 30 archetípusra, és csak a kért nyelvet szerializálja. A `mode`
  fixen `"other-leads"`, mert így EGY futásból megvan az atom-válogatás és a
  vezetői kiegészítő is — a felületen a kapcsoló csak mutatja vagy elrejti,
  hálózat nélkül.
- **`src/components/results/InteractionSection.tsx`** — két `SelectField`
  (legerősebb / második dimenzió, a második kizárja az elsőt), vezető-mód
  kapcsoló, három blokk-kártya, `sparse` állapot és a hitelességi jegyzet.
  Alapértelmezésben az első NEM sparse archetípusra áll — üres állapottal
  indulni azt sugallná, hogy a funkció nem működik.
- **i18n**: 11 új kulcs HU+EN a `results` névtérben.

**Mérés a payload-döntéshez** (a becslés helyett): a 30 archetípus szerializálva
65,4 KB nyers JSON, **gzip alatt viszont 4,1 KB** — az atom-szövegek
archetípusok közt ismétlődnek, ezért szinte teljesen összenyomódnak. Erősen
pólusos profilnál 74,2 KB / 5,5 KB gzip. Ez bőven belefér, tehát a
szerver-oldali előszámítás marad: nulla roundtrip archetípus-váltáskor, és az
~1000 soros atom-tartalom nem kerül a kliens bundle-be.

**Hitelességi jegyzet a felületen** (kötelező volt az F2 döntése miatt): a
szekció kiírja, hogy a típus-prototípusnak csak a két legerősebb dimenziója
ismert, a másik négyről nem állítunk semmit — hipotézis, nem jóslat, és a valós
méréshez a csapat-nézet kell.

**Verifikáció:** 7 új unit teszt (`interaction-view.test.ts`), összesen **378
zöld** (371 → 378), client 64/64, type-check 0 hiba, lint 0 error. Az i18n
kulcsok feloldását külön ellenőriztem mind a 11-re, HU+EN-en — rossz névtérnél
némán a nyers kulcs jelenne meg a felületen.

Az egyik teszt kifejezetten **névtér-őr**: végigmegy az összes archetípus összes
során, és elbukik, ha bármelyik dimenzió-címke nem a kanonikus `tritan.ts`
HEXACO-térképéből jön, vagy ha belső dimenziókód (TEMP/THOR/…) szivárogna ki a
felületre.

Következő: PDF-ízelítő (aug 10) → F4 team view integráció (aug 11–13).
